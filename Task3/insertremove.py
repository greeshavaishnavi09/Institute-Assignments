# Create a list. Insert "Python" at index 0, then remove the last element using pop(), and print the length.

lists = ["is" ,"a", "prog" ,"lan"]
lists.insert(0,"Python")
lists.pop(4)
length_lists = len(lists)
print(lists, length_lists)
